BEGIN;
    CREATE: EX(1,1): MARK(1);

SERV QUEUE,1;
    SEIZE: SERVIDOR;
    DELAY: EX(2,2);
    RELEASE: SERVIDOR;
    COUNT:3;
    BRANCH,1:
        WITH, 0.5, REGIS1:
        WITH, 0.5, REGIS2;

REGIS1  ASSIGN:A(2)=5;
        COUNT:1;
        ASSIGN:A(3)=3:NEXT(REGIS);
REGIS2  ASSIGN:A(2)=6;
        COUNT:2;
        ASSIGN:A(3)=3:NEXT(REGIS);

REGIS QUEUE,2;
    SEIZE: REGISTRADOR;
    DELAY: EX(A(2),2);
    RELEASE: REGISTRADOR;
    TALLY:1, INT(1): DISPOSE;

END;
