BEGIN;
    CREATE: EX(1, 2): MARK(1);
BODE ASSIGN: A(2) = 1;
    QUEUE, 1, 2, PRACTI;
    SEIZE: BODEGUERO;
    DELAY:EX(2, 3);
    COUNT: 1, 1;
    RELEASE: BODEGUERO: NEXT(REGIS);
PRACTI ASSIGN:A(2) = 2;
    QUEUE, 2;
    SEIZE: PRACTICANTE;
    DELAY:EX(3, 4);
    COUNT: 2, 1;
    RELEASE: PRACTICANTE: NEXT(REGIS);
REGIS ASSIGN:A(2) = 3;
    QUEUE, 3;
    SEIZE: REGISTRADOR;
    DELAY:NP(4,4);
    RELEASE: REGISTRADOR: DISPOSE;
END;